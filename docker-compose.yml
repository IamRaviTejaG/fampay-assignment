version: "3"
services:
  app:
    build: ./
    restart: always
    volumes:
      - .:/app
    ports:
      - 5000:5000
    environment:
      - SENTRY_DSN=https://edabbae15c374c92b435a3da7c8aa6e1@o499999.ingest.sentry.io/5579145
      - GOOGLE_API_KEY=AIzaSyC__eatCyC01rG96PoL2CEJLeaaJWegWmU
      - GOOGLE_API_REFRESH_INTERVAL=30
      - HOST=localhost
      - PORT=5000
      - MONGO_HOST=mongo
      - MONGO_PORT=27017
      - MONGO_DBNAME=fampay
      - REDIS_PORT=6379
      - REDIS_HOST=redis
      - YT_SEARCH_QUERY=music
    command:
      sh -c 'npm i && npm start'
    links:
      - mongo
      - redis

  mongo:
    container_name: mongo
    image: mongo
    environment:
      MONGO_INITDB_DATABASE: fampay
    ports:
      - "27017:27017"

  redis:
    container_name: redis
    image: redis
    expose:
      - 6379
